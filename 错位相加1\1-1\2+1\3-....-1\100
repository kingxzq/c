#include <stdio.h>
int main()
{
    int i=0, j=0;
    double sum=0.0, sub=0.0;
    for (i = 1; i < 101; i += 2)
    {
        sum += 1.0 / i;
    }
    for (j = 2; j < 101; j += 2)
    {
        sub += 1.0 / j;
    }
    double put = sum - sub;
    printf("%lf", put);
    return 0;
}
